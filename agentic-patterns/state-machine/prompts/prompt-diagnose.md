# DIAGNOSE State

A difference was found. Your goal is to understand exactly why.

## Steps

1. Open `math_new.py` and read the failing function
2. Compare it line by line against the same function in `math_old.py`
3. Identify the exact bug — wrong operator, extra operation, wrong logic

## Output

- Bug description in one sentence
- Exact line number in `math_new.py`
- What it currently does vs what it should do
- Move to state [FIX]
